# Runtime dependencies — kept in lock-step with pyproject.toml [project.dependencies].
# Prefer `pip install -e .` (which reads pyproject.toml); this file is the flat
# mirror for environments that install requirements directly.
rich>=13.0
psutil>=5.9
jsonschema>=4.17
pexpect>=4.8
watchdog>=3.0
PyYAML>=6.0
pytest>=7.0
